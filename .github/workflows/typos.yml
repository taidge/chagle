name: Typos

on:
  pull_request:
    branches: ["*"]
  push:
    branches: ["main", "dev"]

jobs:
  typos:
    name: Check for typos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: crate-ci/typos@master
